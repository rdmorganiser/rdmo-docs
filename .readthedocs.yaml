version: 2

build:
  os: ubuntu-24.04
  tools:
    # Available Python versions for Read the Docs
    # Ref: https://docs.readthedocs.io/en/stable/config-file/v2.html#build-tools-python
    python: "3.12"

sphinx:
  configuration: docs/conf.py

python:
  install:
    - requirements: requirements.txt
